<Page 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Style="{DynamicResource TextBlockTitle}" Text="{DynamicResource Settings}"/>
        <TextBlock Grid.Row="1" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{DynamicResource lblTest}"/>
        <ComboBox Name="Language" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <ComboBoxItem Content="English" Tag="en"/>
            <ComboBoxItem Content="Français"  Tag="fr"/>
        </ComboBox>
    </Grid>
</Page>